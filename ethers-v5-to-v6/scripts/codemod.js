/**
 * ethers-v5-to-v6 Main Codemod
 * Placeholder for codemod.com registry
 * Full transforms available via npm install ethers-v6-codemod
 */

module.exports = function(fileInfo, api) {
  // Placeholder: actual JSCodeshift transforms are in src/ folder
  // Use via: npm install ethers-v6-codemod or jscodeshift transforms
  return fileInfo.source;
};
