// OLD v5 code — this is what your codemod receives as input
const a = ethers.utils.parseEther('1.0');
const b = ethers.utils.formatEther(value);
const c = ethers.utils.keccak256(data);
const d = ethers.utils.id('hello');
const e = ethers.utils.getAddress(addr);
