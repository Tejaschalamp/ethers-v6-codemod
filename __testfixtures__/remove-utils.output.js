// OLD v5 code — this is what your codemod receives as input
const a = ethers.parseEther('1.0');
const b = ethers.formatEther(value);
const c = ethers.keccak256(data);
const d = ethers.id('hello');
const e = ethers.getAddress(addr);
